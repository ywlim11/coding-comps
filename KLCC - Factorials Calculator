#Factorials Calculator

number = int(input("enter your number: "))
if number % 2 == 0:
    factorial = 1
    numrange = int(number/2)
    for i in range(numrange):
        factorial = factorial*number*(number-1)
        number-=2

    print(factorial)
else:
    factorial = 1
    while number != 1:
        factorial = factorial*number*(number-1)
        number-=2

    print(factorial)
